# ─────────────────────────────────────────────
# Doer IAM — Keycloak Configuration
# ─────────────────────────────────────────────

# Database
db=postgres
db-username=keycloak_user
db-password=keycloak_pass_2026
db-url=jdbc:postgresql://localhost:5432/keycloak

# HTTP (dev mode — TLS in production)
http-enabled=true
http-port=8080
hostname=localhost

# Observability
health-enabled=true
metrics-enabled=true

# Logging
log-level=info
